# Database connection string from Neon
DATABASE_URL=postgresql://user:password@host.region.neon.tech/database?sslmode=require

# Environment (production or development)
NODE_ENV=production

# PORT is only for local development - Render will set this automatically in production
# Do NOT set PORT when deploying to Render
PORT=5000
